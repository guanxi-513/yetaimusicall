# 液态音乐（Yetai Music）更新日志

- **版本**：v1.1.0
- **更新日期**：2026-09-10
- **仓库结构**：`frontend/`（Flutter App）+ `backend/`（Node 音源服务端）

---

## 一、本次更新范围

| 端 | 说明 |
| --- | --- |
| `backend/` | 新增酷狗、QQ 音乐音源模块，强化受限歌曲换源链路 |
| `frontend/` | 同步多音源支持（酷狗 / QQ 登录、歌单、每日推荐、收藏）与歌词、换源播放等前端实现 |

> ⚠️ **汽水音乐登录：本次暂缓**。公开仓库不包含汽水音乐相关模块（`soda-api.js` 不上传），后端 `server.js` 已做兼容处理：未安装汽水模块时 `/soda/*` 接口统一返回 404，不影响其他音源使用。前端 App 中的汽水入口保留但暂不可用，后续接入时再随更新提交。

---

## 二、后端新增接口（`backend/`）

### 酷狗音乐（第 2 音源，`kugou-api.js`）

| 接口 | 说明 |
| --- | --- |
| `GET /kugou/search?keywords=&limit=` | 酷狗搜索 |
| `GET /kugou/song/url?id=&br=` | 酷狗歌曲播放地址 |
| `GET /kugou/login/qr` | 酷狗扫码登录（取码） |
| `GET /kugou/login/qr/check?key=&force=` | 轮询扫码状态 |
| `GET /kugou/user/playlist` | 我的歌单 |
| `GET /kugou/playlist/detail?id=` | 歌单详情 |
| `GET /kugou/recommend/daily` | 每日推荐（30 首） |
| `GET /kugou/recommend/fm` | 猜你喜欢（FM 推荐） |
| `GET /kugou/status` / `GET /kugou/logout` | 登录态查询 / 退出 |

### QQ 音乐（第 3 音源，`qq-api.js`）

| 接口 | 说明 |
| --- | --- |
| `GET /qq/login/qr` | QQ 音乐扫码登录（取码） |
| `GET /qq/login/qr/check?key=&force=` | 轮询扫码状态 |
| `GET /qq/user/playlist` | 我的歌单 |
| `GET /qq/like/playlist` | 我喜欢（收藏歌单） |
| `GET /qq/like?id=&action=` | 红心收藏 / 取消（同步 QQ 收藏） |
| `GET /qq/playlist/detail?id=` | 歌单详情 |
| `GET /qq/recommend/daily` | 每日推荐（30 首） |
| `GET /qq/song/url?id=&br=` | 歌曲播放地址 |
| `GET /qq/status` / `GET /qq/logout` | 登录态查询 / 退出 |

### 换源链路（`frontier.js` + `frontier-fws.esm.js`）

- 受限 / VIP 歌曲按顺序换源：**当前歌单平台源 → 网易云解锁源 → QQ 音乐源 → B站兜底**
- 匹配规则：歌名 + 歌手强匹配优先，失败回退时长匹配（偏差 ≤ 35s）
- B站兜底优先：时长相近、标题含"无损音质 / 某某视听版本"的官方/高音质源

### 既有接口（不变）

`/search`、`/search/bili`、`/song/url`、`/song/url/unblock`、`/song/url/bili`、`/lyric`、`/lyric/any`、`/song/detail`、`/playlist`、`/user/playlist`、`/like`、`/likelist`、`/login/qr`、`/login/qr/check`、`/recommend`（网易云）

---

## 三、前端（`frontend/`）

- 支持多音源登录：网易云 / 酷狗 / QQ（各自独立登录态，互不覆盖）
- 歌单页：按平台分组展示已登录平台的歌单，未登录平台显示登录引导卡片
- 每日推荐：各平台登录后展示对应每日推荐（网易云 / 酷狗 / QQ）
- 红心收藏：QQ 歌曲收藏同步到 QQ 音乐收藏；本地收藏与平台收藏分离
- 歌词、换源播放、后台播放、通知栏控制、毛玻璃 UI 保持不变

---

## 四、部署

```bash
cd backend
npm install
npm start        # 默认端口 41831，可用 PORT 环境变量修改
```

验证：

```bash
curl http://localhost:41831/status
curl "http://localhost:41831/kugou/search?keywords=周杰伦&limit=5"
curl "http://localhost:41831/qq/recommend/daily"   # 需先登录 QQ
```

前端：

```bash
cd frontend
flutter pub get
flutter run
```

App「设置」中填写音源服务地址（如 `http://服务器IP:41831`）。

---

## 五、安全与说明

- 各平台登录态（cookie）保存在服务器本地，**不随仓库上传**；`.gitignore` 已覆盖所有 cookie / 日志文件
- 逆向调试产物（抓包、APK、证书等）**不随仓库上传**
- 本项目仅用于个人学习与技术交流，请遵守各平台服务条款，勿商用、勿绕过付费内容
