# UPDATE 2026-09-10 · v1.2.0

> 本次更新：**UI 风格重做（黑底青绿光 Spotify 风）** + **播放页/歌单详情页全透明** + **歌曲卡片去模糊（性能大优化）** + 设置页新增「自定义界面」开关。
> 日期：2026-09-10 ｜ 仓库结构：`frontend/`（Flutter App）+ `backend/`（Node 音源服务）

## 本次变更

### 1. UI 风格重做：黑底青绿光（Spotify 极简暗色风）

**背景**：原全局背景是深空蓝紫渐变 + 4 个径向光斑（紫/暗粉），用户反馈"不搭、丑"。

**改动（前端）**：
- 新建 `lib/widgets/spotify_background.dart`：纯黑→深炭灰渐变底 + 左侧青绿光（`#1DB954`，0.40→0.10→透明，弥散到约 60% 屏宽），替换原根背景
- 设置弹窗背景同步从紫色 `#251B3D` 改为深黑 `#121216`，与新风格统一

### 2. 播放页 / 歌单详情页：去掉截图模糊，改全透明

**背景**：此前为"透出下层"引入了截图机制（进入页面前截取上一页画面 → 模糊铺底），视觉上"怪怪的"，且截图+模糊开销大。

**改动（前端）**：
- 移除 `lib/services/page_snapshot.dart`（整个截图工具删除）
- `player_page.dart` / `playlist_detail_page.dart`：背景层改为全透明（两个页面路由本来就是 `opaque: false`，直接透出下层页面）
- 移除 `recommend_view.dart` / `mini_player_bar.dart` / `home_page.dart` 中的截图调用与 `RepaintBoundary`

### 3. 歌曲卡片去模糊：列表性能大优化（本次最大收益）

**背景**：歌单/搜索/榜单/收藏列表里每首歌曲卡片都是一个 `BackdropFilter`（毛玻璃背景模糊），几百个列表项逐个模糊是性能大头（真机明显卡顿，模拟器流畅）。

**改动（前端）**：
- `glass_card.dart`：新增 `blur` 开关（默认 `true`），`false` 时跳过 `BackdropFilter`，只画半透明卡片（保留渐变底色、圆角、描边、阴影）
- `song_tile.dart`：歌曲卡片默认**不渲染**模糊（保留半透明白渐变卡片样式）
- 实测：真机滚动流畅度显著提升

### 4. 设置页新增「自定义界面」

- 右上角设置弹窗新增「自定义界面」分区：**歌曲卡片毛玻璃**开关
- 开启 = 卡片带背景模糊（还原旧效果）；关闭 = 纯半透明卡片（默认，省性能）
- 状态用 `shared_preferences` 持久化（`lib/state/ui_settings.dart`），切换即时生效、重启不丢

## 文件变更清单

| 文件 | 变更 |
|---|---|
| `frontend/lib/widgets/spotify_background.dart` | **新增**：黑底青绿光根背景组件 |
| `frontend/lib/state/ui_settings.dart` | **新增**：UI 设置全局状态 + 持久化 |
| `frontend/lib/widgets/glass_card.dart` | 新增 `blur` 开关（去模糊开关） |
| `frontend/lib/widgets/song_tile.dart` | 歌曲卡片读取全局 blur 开关，默认关闭 |
| `frontend/lib/main.dart` | 根背景换 SpotifyBackground + 启动加载 UI 设置 |
| `frontend/lib/pages/home_page.dart` | 设置弹窗新增「自定义界面」+ 弹窗背景改深黑 |
| `frontend/lib/pages/player_page.dart` | 播放页去截图模糊，全透明透出下层 |
| `frontend/lib/pages/playlist_detail_page.dart` | 歌单详情页去截图模糊，全透明透出下层 |
| `frontend/lib/pages/recommend_view.dart` | 移除截图调用 |
| `frontend/lib/widgets/mini_player_bar.dart` | 移除截图调用 |
| `frontend/lib/services/page_snapshot.dart` | **删除**：截图工具整体移除 |
| `README.md` | 更新日志新增 v1.2.0 |

## 验证方式

1. 首页背景为黑底 + 左侧青绿弥散光（约 60% 屏宽）
2. 点击歌曲进播放页：背景**全透明**，可看到下层首页青绿光；下拉关闭动画正常
3. 点开歌单详情：同样全透明透出下层
4. 歌单/搜索/榜单列表滚动：真机明显更流畅（歌曲卡片不再逐个模糊）
5. 设置 → 自定义界面 → 打开「歌曲卡片毛玻璃」：列表恢复模糊效果；关闭恢复简洁；重启后开关状态保留
