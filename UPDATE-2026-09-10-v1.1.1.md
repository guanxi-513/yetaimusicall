# UPDATE 2026-09-10 · v1.1.1

> 本次更新：**网易云登录态隔离修复** + B站直连增强 + 汽水模块可选加载。
> 日期：2026-09-10 ｜ 仓库结构：`frontend/`（Flutter App）+ `backend/`（Node 音源服务）

## 本次变更

### 1. 修复：网易云登录态串号（重要）

**现象**：别人的网易云账号登录后，其他设备的歌单/头像变成那个人。

**根因**：登录成功后 cookie 只写入服务器全局 `cookie.txt`，**没有回传给客户端**。客户端本地永远没有 cookie → 所有请求不带 Cookie 头 → 后端全部回退"全局最后登录者"——谁最后扫码，所有设备显示谁的账号。

**修复（后端，已改）**：
- `POST/GET /login/qr/check`：登录成功（`code == 803`）时返回 `cookie` 字段（登录态整串）
- 客户端保存后，后续请求自带 Cookie 头 → 后端"请求 cookie 优先" → **多设备各用各的账号，互不覆盖**

**配套（前端，见 `docs/网易云登录态隔离-前端提示词.md`）**：
- `loginQrCheck` 返回 `{code, cookie}`
- 新增 `setNeteaseCookie` 整串保存到本地
- 登录成功 803 分支先保存 cookie 再刷新登录态

> QQ / 酷狗 / 汽水登录态早已"整串保存 + 请求自带"，本次无需改动，天然隔离。

### 2. B站音源直连播放增强（后端）

- `/song/url/bili` 返回 `directUrl`（B站 CDN 直链）+ `headers`（Referer / UA / 游客 cookie）
- App 直连播放，省去服务器转发带宽；直连失败自动回退服务器转发
- 前端接入见 `docs/B站直连播放-前端提示词.md`（待发）

### 3. 汽水音乐模块可选加载（开源版）

- `server.js` 对 `soda-api.js` 改为可选加载：仓库版无该文件时 `/soda/*` 返回 404，**不影响其他音源**
- 汽水登录态 / 逆向产物 / 工作日志**均不上传**（见 `.gitignore`）

## 文件变更清单

| 文件 | 变更 |
|---|---|
| `backend/server.js` | 登录 cookie 回传 + B站直连 + soda 可选加载（+31 行） |
| `README.md` | 更新日志新增 v1.1.1 |
| `UPDATE-2026-09-10-v1.1.1.md` | 本次更新说明（本文档） |

## 验证方式

1. 设备 A 扫码登录账号 A → 显示 A 的歌单/头像
2. 设备 B 扫码登录账号 B → 显示 B 的
3. 回到设备 A 杀掉重开 → **仍显示 A**（cookie 已持久化到本地）
4. 两个设备可同时在线，互不覆盖
